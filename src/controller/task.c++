import std::fmt 
class Name{
    private str getMain(){
        system.out.println('')
    }
    std::thread int<num> getTx(large large n){
        std::thread name()
    }
    list<string>collection= arraylist<string>()
    for i:collection{
        //-> pointers memory storage, library //vectors
        set<int>getNumbers = hashset<int>()
        for i: getNumbers{
            //transversing through a collection

        }
    };
};